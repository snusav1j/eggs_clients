.box
  .box-header
    .box-header-title
      = tm(Interaction, :title)
      %span.box-header-count.text-muted= @interactions.length > 0 ? @interactions.length : ''
    = link_to new_modal_interactions_path, class: "btn btn-primary", remote: true do 
      = fa_icon(:plus)
      = tc(:new)

  .box-body
    %table
      %thead
        %tr
          %th= tm(Interaction, :client)
          %th= tm(Interaction, :interaction_type)
          %th= tm(Interaction, :status)
          %th= tm(Interaction, :comment)
          %th= tm(Interaction, :manager)
      %tbody#interactions-list
        - @interactions.each do |interaction|
          %tr.interaction-row{ data: { interaction_id: interaction.id } }
            %td= interaction.client&.name
            %td= interaction.interaction_type_name
            %td= interaction.status
            %td= interaction.comment
            %td= interaction.manager&.email

#interaction-modal-outer